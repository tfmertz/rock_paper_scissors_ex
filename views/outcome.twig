<!DOCTYPE html>

<html>
    <head>
        <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="/css/styles.css" media="screen" title="no title" charset="utf-8">
        <title>Winner!</title>
    </head>
    <body>
        <div class="container">
            <div class="row">
                <div class="col-md-12">

            {% if winner == "Tie" %}

                    <h1>You're BOTH winners! :D</h1>

                </div>
            </div>

            <div class="row">
                <div class="col-sm-5 button-field-height selected">
                    <h3>You tied player 2!</h3>
                    <p>{{player1}}</p>

                </div>
                <div class="col-sm-5 col-sm-offset-2 button-field-height selected">
                    <h3>You tied player 1!</h3>
                    <p>{{player2}}</p>
                </div>
            </div>

            {% endif %}

            {% if winner == "Player 1" %}

                    <h1>Player 1 wins!</h1>

                </div>
            </div>

            <div class="row">
                <div class="col-sm-5 button-field-height selected">
                    <h3>Winner!</h3>
                    <p>{{player1}}</p>

                </div>
                <div class="col-sm-5 col-sm-offset-2 button-field-height loser">
                    <h3>You lost to player 1!</h3>
                    <p>{{player2}}</p>
                </div>
            </div>

            {% endif %}

            {% if winner == "Player 2" %}

                    <h1>Player 2 wins!</h1>

                </div>
            </div>

            <div class="row">
                <div class="col-sm-5 button-field-height loser">
                    <h3>You lost to player 2!</h3>
                    <p>{{player1}}</p>

                </div>
                <div class="col-sm-5 col-sm-offset-2 button-field-height selected">
                    <h3>Winner!</h3>
                    <p>{{player2}}</p>
                </div>
            </div>

            {% endif %}

            <div class="row">
                <div class="col-md-12">
                    <p>
                        <a href="/" class="btn btn-primary">Play again!</a>
                    </p>
                </div>
            </div>

        </div>
    </body>
</html>
